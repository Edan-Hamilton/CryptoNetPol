blockArgs: &blocked
  protocols: { bannedCommandStrings: [ stratum+ ] }
  pools: { bannedCommandStrings: [ pool., mine., .mine ] }
  algos: { bannedCommandStrings: [ %s ] }
cryptnono: { detectors: { execwhacker: { configs: *blocked } } }
